{
	"name": "auctions-subgraph",
	"license": "UNLICENSED",
	"scripts": {
		"codegen": "graph codegen",
		"build": "graph build",
		"deploy": "graph deploy --node https://api.thegraph.com/deploy/ @llama/auctions-subgraph",
		"format": "pnpm rome format **/* --write",
		"lint": "pnpm rome check **/* --apply-unsafe"
	},
	"dependencies": {
		"@graphprotocol/graph-cli": "0.49.0",
		"@graphprotocol/graph-ts": "0.29.1"
	},
	"devDependencies": {
		"matchstick-as": "0.5.0",
		"rome": "^12.1.0"
	}
}
