type Auction @entity(immutable: false) {
  id: Bytes!
  llama_id: BigInt!
  start_time: BigInt!
  end_time: BigInt!
  total_bids: BigInt!
  latest_bid_amount: BigInt!
  latest_bidder: Bytes!
}

type CurrentAuction @entity(immutable: false) {
  id: Bytes!
  llama_id: BigInt!
  start_time: BigInt!
  end_time: BigInt!
  total_bids: BigInt!
  latest_bid_amount: BigInt!
  latest_bidder: Bytes!
}

type AuctionGlobalConfiguration @entity(immutable: false) {
  id: Bytes!
  min_bid_increment_percentage: BigInt!
  reserve_price: BigInt!
  time_buffer: BigInt!
  duration: BigInt!
}